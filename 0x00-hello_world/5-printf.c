#include <stdio.h>

/**
 * main - This serves as a starting point for program execution
 *
 * Return: always takes the value of 0
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
